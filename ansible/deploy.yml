---
- name: Deploy to Tomcat
  hosts: web
  become: yes
  tasks:
    - name: Copy WAR file to Tomcat
      copy:
        src: "/var/snap/jenkins/4865/workspace/MyMavenWebAppWithAnsible-CICD/target/mvn_web_app.war"
        dest: "/opt/tomcat/webapps/mvn_web_app.war"
        remote_src: no

    - name: Restart Tomcat
      systemd:
        name: tomcat
        state: restarted
